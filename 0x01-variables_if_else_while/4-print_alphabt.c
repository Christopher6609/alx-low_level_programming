#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int ch;
 while (ch <= 'z')
   {
     if (ch == e || ch == q)
       {
	 ch++;
	 continue;
       }
     putchar(ch);
     ch++;
   }
return (0);
}
