#include "main.h"
/**
 * _strncpy - copies a string with n bytes
 * @dest: copy to
 * @sr: copy from
 * @n: number of character
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
int j = 0;
while (j < n && src[j] != '\0')
{
dest[j] = src[j];
j++;
}
dest[j] = '\0';
return (dest);
}
